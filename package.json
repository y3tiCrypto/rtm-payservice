{
  "name": "raptoreumpay-frontend",
  "version": "1.6.0",
  "description": "Production frontend assets and formatting for RaptoreumPay",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "format": "prettier --write \"static/**/*.{js,html}\"",
    "format:check": "prettier --check \"static/**/*.{js,html}\""
  },
  "devDependencies": {
    "prettier": "^3.2.5"
  }
}
